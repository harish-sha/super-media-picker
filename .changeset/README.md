# Changesets

Run `pnpm changeset` for a user-visible package change. The root workspace is
private. `super-media-picker` is the public package; scoped workspace packages
remain internal release inputs unless a separate release plan explicitly makes
them public.
