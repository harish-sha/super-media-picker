# @company/media-core

Framework-independent media item models, provider contracts, feature and capability configuration, storage, caching, analytics, and normalized errors.

This package has no React dependency and is safe to import during server rendering.
